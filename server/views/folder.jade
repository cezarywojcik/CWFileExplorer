document
  listTemplate
    banner 
      title CWFileExplorer
    list
      header 
        text #{ title }
      section
        each item in items
          listItemLockup(type=item.type, path=item.path)
            title #{ item.title }
            relatedContent
              lockup
                title #{ item.title }
                // img(src=item.image)
                description #{ item.type }

